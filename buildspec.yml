version: 0.2

phases:
  build:
    commands:
      - dotnet publish
artifacts:
  files:
    - ./JuniorTennis.Mvc/bin/Debug/netcoreapp3.1/publish/**/*
    - ./appspec.yml
    - ./before-install.bat
    - ./after-install.bat