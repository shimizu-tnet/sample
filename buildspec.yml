version: 0.2

phases:
  build:
    commands:
      - dotnet restore
      - dotnet publish
artifacts:
  files:
    - ./JuniorTennis.Mvc/bin/Debug/netcoreapp3.1/*
    - ./appspec.yml
    - ./before-install.bat
    - ./after-install.bat